# Arithmatic Expression Lab
### Usage
Run `dotnet run -p ArithmaticExpressionLab` in your terminal. Type your expressions. (Only simple add and subtract operations are supported at the moment.) Press `Ctrl + C` to exit.